<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.stock.notification.dao.StockDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.stock.notification.entity.StockEntity" id="stockMap">
        <result property="stockname" column="stockname"/>
        <result property="stockcode" column="stockcode"/>
        <result property="areacode" column="areacode"/>
        <result property="stocktype" column="stocktype"/>
        <result property="contribution" column="contribution"/>
        <result property="totalvalue" column="totalvalue"/>
        <result property="totalissue" column="totalissue"/>
    </resultMap>

    <select id="findAllStock">

    </select>

</mapper>
